
## 新增 heading-4/5/6（block 分类，152→155）
- 源文件 `icons/block/heading-{4,5,6}.svg`，左侧 H 字形与 h1–h3 逐字节相同，右侧数字新画。
- 三轮迭代：v1 的 5（碗浅）糊、6（上弧+大圈）读成 2；v2 的 6 读成 C；v3 定稿 = 4（竖+斜+横，顶 7.5/横线 y14）、5（顶横左竖+深碗到底 18）、6（闭合整圆 + 长弧接环上左上 45° 点）。
- 复核方式：本地复验页 + agent-browser 截图对比 20px/48px；注意复验页 innerHTML 注入的 svg 需手动补 `width/height=1em` 否则渲染 0×0，且多段路径 split('|') 只取前两段的坑。
- 教训：改源文件后必须重跑 gen（中途发现 src/icons 未同步 diff 失败一次）。
- 同步：icons-meta.json（zh/en/keywords/tags/status）、README 总数 155 + 分类表修正（README 分类数量此前已与实际脱节，一并按 gen 输出修正）、AGENTS.md 规模行、MEMORY.md。

## 修正 h4/h6（Tiny 反馈）
- h4：横线 `h4.3`→`h6`，向右出头到 x=20.7（过竖线 1.7 单位）。
- h6：v3 只让弧端点落在环上但切线不连续 → 读成 C；改为长弧终点=环 135° 点 (15.45,13.05)、终止切向=(−0.707,+0.707) 与环切向严格一致 + 四段 90° C 整环（k·r=1.6），弧→环无缝。
- 候选法教训：弧接在环右上 45°/60°/顶部的三个候选全读成 d——6 的长弧必须扫到环左上。
- 手滑写错过一次 heading-6 源文件（没用验证坐标），靠「从生成物提取 d 终验」兜住；终验应固定从 src/icons/*.vue 提取而非手抄。

## 固化「数量同步」约定 + 补漏
- Tiny 要求：每次图标数量变化都要同步文档计数，并写入 AI 记忆。
- 发现 README 第 138 行概述段总数仍是「152」（上轮只改了分类表），已改为 155。
- MEMORY.md「分类与元数据维护」段已强化为【强制】规则，点出 README 有「概述总数 + 分类表」两处计数（易漏），并以 gen 输出为准。

## 预览站计数徽标漏改
- Tiny 指出 hero 徽标还是「152 个图标」。
- SiteHero.vue 徽标改为动态 `stats.total`/`stats.categories`（来自 useIconLibrary 的 allIcons.length），一劳永逸；index.html 的 meta description 手改 152→155。
- 记忆同步清单补上第 4 条：SiteHero 徽标已动态化（免改）、index.html meta 要手改、dist-site 是产物勿改。

## 部署文档站到服务器
- `npm run build:site` 重新构建（155 图标）→ `rsync -avh --delete dist-site/ root@8.134.71.36:/www/wwwroot/richtext-icons.wzt.zone`。
- 验证：线上 https://richtext-icons.wzt.zone/ meta description 已显示「155 个图标」。
- 注意：rsync --delete 删除服务器 `.user.ini`（宝塔保护文件）会报 Operation not permitted，属正常，不影响部署。
- npm 发布：0.3.0 因 2FA TOTP（auth-and-writes）卡住，Tiny 改用浏览器/系统钥匙串自行发布，最终 npm 线上仍停在 0.2.0（未确认是否成功）。发布前 build:lib 已完成、版本已 bump 0.3.0（package.json + 无 git tag）。
